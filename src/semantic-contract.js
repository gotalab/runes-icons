export const CONCEPT_KINDS = Object.freeze([
  "entity",
  "action",
  "state",
  "artifact",
  "relationship",
  "container",
  "capability",
  "metric",
  "pattern",
]);

export const ICON_CONCEPT_KINDS = Object.freeze({
  "agent-as-tool": "pattern",
  handoff: "action",
  delegation: "action",
  orchestrator: "pattern",
  routing: "action",
  "planner-agent": "entity",
  "agent-team": "pattern",
  "agent-trio": "pattern",
  "agent-swarm": "pattern",
  "parallel-agents": "pattern",
  model: "artifact",
  inference: "action",
  "model-serving": "capability",
  "model-registry": "container",
  "model-checkpoint": "artifact",
  checkpoint: "state",
  "prompt-cache": "capability",
  "semantic-cache": "capability",
  "vector-store": "container",
  retrieval: "action",
  grounding: "action",
  evidence: "artifact",
  citation: "artifact",
  provenance: "relationship",
  activation: "metric",
  engagement: "metric",
  retention: "metric",
  stickiness: "metric",
  churn: "metric",
  goal: "state",
  plan: "artifact",
  task: "artifact",
  check: "action",
  done: "state",
});

export const SEMANTIC_DISTINCTION_GROUPS = Object.freeze([
  Object.freeze({ id: "agent-control", members: Object.freeze(["agent-as-tool", "handoff", "delegation"]), distinction: "Nested invocation retains caller control; handoff transfers active control; delegation dispatches bounded work." }),
  Object.freeze({ id: "agent-coordination", members: Object.freeze(["orchestrator", "routing", "planner-agent"]), distinction: "Orchestrator owns coordination; routing chooses a branch; planner-agent produces a plan." }),
  Object.freeze({ id: "agent-grouping", members: Object.freeze(["agent-team", "agent-trio", "agent-swarm", "parallel-agents"]), distinction: "Team is a collaboration structure; trio is a fixed preset; swarm is emergent; parallel-agents is an execution pattern." }),
  Object.freeze({ id: "model-lifecycle", members: Object.freeze(["model", "inference", "model-serving", "model-registry", "model-checkpoint"]), distinction: "Model is an artifact; inference executes it; serving exposes it; registry catalogs it; checkpoint saves one state." }),
  Object.freeze({ id: "checkpoint-kinds", members: Object.freeze(["checkpoint", "model-checkpoint"]), distinction: "Checkpoint anchors workflow restoration; model-checkpoint is a saved model artifact." }),
  Object.freeze({ id: "cache-and-retrieval", members: Object.freeze(["prompt-cache", "semantic-cache", "vector-store", "retrieval"]), distinction: "Prompt cache reuses exact computation; semantic cache reuses meaning-equivalent results; vector store persists an index; retrieval searches it." }),
  Object.freeze({ id: "evidence-and-source", members: Object.freeze(["grounding", "evidence", "citation", "provenance"]), distinction: "Grounding is an anchoring action; evidence is inspectable proof; citation marks one source; provenance records lineage." }),
  Object.freeze({ id: "product-lifecycle", members: Object.freeze(["activation", "engagement", "retention", "stickiness", "churn"]), distinction: "Each metric answers a different lifecycle question: first success, repeated activity, continued cohort presence, frequency ratio, or departure." }),
  Object.freeze({ id: "work-intent", members: Object.freeze(["goal", "plan", "task"]), distinction: "Goal is a destination state; plan is an intended sequence artifact; task is one bounded work unit." }),
  Object.freeze({ id: "confirmation-and-completion", members: Object.freeze(["check", "done"]), distinction: "Check confirms locally; done is a terminal success state." }),
]);
